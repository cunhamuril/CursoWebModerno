==========================================================================================================================================================
|													                  COMANDOS GIT 																		 |
==========================================================================================================================================================

"https://rogerdudler.github.io/git-guide/index.pt_BR.html" // guia sem complicação
"https://gist.github.com/leocomelli/2545add34e4fec21ec16"

--- DIZENDO SEU NOME NO GIT ---

$ git config //global user.name 'Your Name Comes Here'
$ git config //global user.email 'you@yourdomain.example.com'



$ git clone <url> // Clonar repositório


--- CRIAR REPOSIÓTIO ---

$ mkdir project // vai criar a pasta project
$ cd project // entra na pasta project
$ git init // iniciar

$ git add . /* vai subir todos os arquivos da pasta atual no primeiro commit: */ ;

------------------------

$ git log // chega o histórico de commits

$ git checkout // Checa o git

$ git show OU $ git show <commit> // Mostra as mudanças do ultimo commit ou do commit especificado

$ git reset --hard <commit> OU git reset HEAD --hard // Volta no commit especificado ou reseta para o commit anterior(head)

------------------------

$ git commit -a -m "comentários das alterações" // fazendo commit -a(all -> todos)

$ git push // enviar alterações para repositório remoto

